// user [] to define a array in JS

var numbers = [10 , "Nitin" , {}, true]

console.log(numbers);

// Aarry is alos a object in JS and it's having it's own methos as well

console.log(numbers.length); // last index + 1 => 4

numbers[100] ="gaikwad";

console.log(numbers.length); //101